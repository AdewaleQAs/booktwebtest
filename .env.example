# Base URL for the application under test
# Staging: https://staging.organizer.getbookt.io
# Local:   http://localhost:3000
BASE_URL=https://staging.organizer.getbookt.io

# Environment (local, staging, production)
ENV=staging

# Test credentials (use test accounts only)
# These are required for authenticated tests (dashboard, modules)
# The auth.setup.ts fixture uses these to log in and save session state
TEST_USER_EMAIL=test@example.com
TEST_USER_PASSWORD=testpassword123

# Optional: Test phone number (must be verified in the system for OTP tests)
TEST_USER_PHONE=1234567890
